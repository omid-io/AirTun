# 🏛️ معماری فنی سیستم AirTun (AirTun Architecture Specification)

سند معماری جامع ابزار **AirTun** برای اشتراک‌گذاری پایدار و کامل اینترنت از گوشی اندروید به رایانه ویندوز.

---

## ۱. نمای کلی سیستم (System Overview)

برخلاف معماری‌های نامتعادل و پرباگ که سعی در شبیه‌سازی سرورهای سنگین روی موبایل دارند، **AirTun** بر پایه اصل «سبک‌ترین بار روی موبایل، بیشترین قدرت روی دسکتاپ» طراحی شده است:

```
┌────────────────────────────────────────────────────────┐
│                   📱 Android Mobile                    │
│                                                        │
│  • هسته سرور: SOCKS5 پروکسی فوق‌سبک (NIO Socket)        │
│  • ترافیک ورودی: Wi-Fi Hotspot / Local LAN             │
│  • ترافیک خروجی: از VPN فعال فعلی گوشی رد می‌شود       │
│  • کاوش محلی: UDP Beacon Broadcast (پورت 47880)        │
│  • امنیت: پین‌کد ۴ رقمی موقت و احراز هویت Handshake    │
│  • مصرف منابع: رم < 15MB ، باتری ~0%                   │
└───────────────────────────┬────────────────────────────┘
                            │ Wi-Fi / Hotspot (LAN)
┌───────────────────────────▼────────────────────────────┐
│                   💻 Windows Client                    │
│                                                        │
│  • رابط کاربری: مدرن WinUI 3 (وسط صفحه، Dark/Light)    │
│  • ناوبری: Rail عمودی سمت راست با ۶ تب:                 │
│      اتصال | DNS | AI Access | مسیریابی | گزارشها | تنظیمات │
│  • حالت اصلی (Full TUN Mode):                           │
│      - ساخت کارت شبکه مجازی WinTun                     │
│      - فوروارد ترافیک با لایه سریع Tun2socks           │
│      - پشتیبانی ۱۰۰٪ از TCP, UDP, DNS, ICMP            │
│      - پوشش بازی‌ها، تلگرام، گیت، ترمینال، مرورگرها    │
│  • حالت فرعی (Fast Proxy Mode):                        │
│      - تنظیم موقت System Proxy ویندوز برای وبگردی      │
│  • لایه ایمنی (Safe Exit):                             │
│      - بازگردانی خودکار کارت شبکه و عدم قطعی اینترنت   │
└────────────────────────────────────────────────────────┘
```
