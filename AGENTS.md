# AGENTS.md — airtun (E:\programming\airtun)

پروژهٔ مستقل خود کاربر: تونل SOCKS5 روی Wintun برای ویندوز.

## مرزها و مالکیت
- شاخهٔ مرجع: `main` (نسخهٔ پایدار v1.1.0، SOCKS5+Wintun).
- شاخه‌های `dev`، `backup-dev` و تگ‌های v1.2+ آثار آزمایش Relay دیگران‌اند — دست نزن، merge/push نکن.
- قبل از هر کاری `pwd`/شاخه چک شود که در main هستیم.

## باگ شناخته‌شده: اشتراک اینترنت (Share net)
- علامت: وقتی از طریق هیدیفای/v2rayNG به تونل وصل می‌شود، IP خودِ گوشی برمی‌گردد نه IP خروجی تونل.
- تشخیص فنی: bind ناقص UDP/DNS در مسیر اشتراکی + احتمال رفتار Proxy-Only کلاینت موبایل.
- راه‌حل برنامه‌ریزی‌شده: پیاده‌سازی Upstream Proxy به ۱۲۷.۰.۰.۱ تا ترافیک موبایل از داخل تونل خارج شود.
